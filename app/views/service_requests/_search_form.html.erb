<div class="panel">
  <div class="panel-body">
    <h3>Busqueda Avanzada</h3>

    <%= form_tag service_requests_path, :method => 'get' do %>
    <div class="form-group">
      <%= label_tag :search, "Busqueda por título" %>
      <%= text_field_tag :search, params[:search], class: "form-control input-sm search" %>
      <%= hidden_field_tag :search_type, "1" %>
      <br>
      <%= submit_tag "Buscar", class: 'btn btn-info' %>
    </div>
    <% end %>

    <hr>

    <%= form_tag service_requests_path, :method => 'get' do %>
    <div class="form-group">
      <%= label_tag :title, "Busqueda por etiqueta" %>
      <br>
      <% @tags.each do |tag|  %>
        <%= label_tag string_to_symbol("tag  #{tag.id}"), tag.name %>
        <%= radio_button_tag(:tag, tag.id) %>
      <% end %>
      <%= hidden_field_tag :search_type, "2" %>
      <br>
      <%= submit_tag "Buscar", class: 'btn btn-info' %>
    </div>
    <% end %>

  </div>
</div>